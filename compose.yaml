services:
    app:
        image: ghcr.io/mateenunez/its-mytabs:latest
        ports:
            # Host Port:Container Port
            - "47777:47777"
        volumes:
            # Host Path:Container Path
            - ./data:/app/data
        restart: unless-stopped
